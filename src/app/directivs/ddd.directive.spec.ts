import { DddDirective } from './ddd.directive';

describe('DddDirective', () => {
  it('should create an instance', () => {
    const directive = new DddDirective();
    expect(directive).toBeTruthy();
  });
});
